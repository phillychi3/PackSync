
## 優先補完

### P0：直接影響主要使用體驗

1. **行程檢視仍未完整** — 🟡 部分完成

文件要求日曆檢視、滾動時間表與地圖同步：[project.md](/C:/Users/whitecloud/Documents/code/PackSync/project.md:49)。

✅ 已完成：

- 「全部 / D1 / D2…」日期切換，依旅程起訖日與行程日期自動產生，切換時自動預填新增表單日期。
- 地圖依選定日期過濾：選單日只顯示當天地點與路線；「全部」時未排入行程的地點以半透明標記顯示。
- 地圖標記與行程卡片雙向互動：點標記捲動並高亮卡片；卡片上的定位按鈕聚焦地圖標記並開啟資訊視窗。
- 「清單／時間表」檢視切換：時間表以小時精度呈現選定日期的行程（依開始／結束時間定位、全天行程另列），點擊時間格行程會聚焦地圖。
- 真實路線：相鄰地點間透過公開 OSRM 服務取得實際道路路線（實線）；查詢失敗或長距離（航班等）退回虛線直線。

❌ 仍缺：

- 月曆型日曆檢視。
- 捲動行程卡片時地圖自動聚焦（目前是點擊觸發，非捲動同步）。
- 路線依交通方式區分（步行／大眾運輸；目前一律以開車路線計算）。

現有基礎可見於 [itinerary/+page.svelte](/C:/Users/whitecloud/Documents/code/PackSync/src/routes/(app)/trips/[tripId]/itinerary/+page.svelte)。

2. **通知目前不是真正的自動提醒系統** — ✅ 已完成

- 通知偏好設定：行程／重要物品／待付款／待辦四類開關與提前提醒時間（`notification_preference`），UI 在通知中心側欄。
- 排程推播端點 `POST /api/push/dispatch`：依每位訂閱者的偏好推播即將開始的行程、未確認重要物品、到期待辦與待付款；`notification_log` 防止重複發送；失效訂閱自動清除。
- 點擊通知深層連結到對應頁面（service worker `notificationclick` 已支援）。
- **事件型即時推播**：新帳單、新成員加入、結算完成（付清）會即時推播給相關成員（排除操作者本人；帳務類尊重「待付款提醒」偏好）。
- **已讀／未讀**：通知改由伺服器端 `buildNotifications` 統一計算，`notification_read` 記錄已讀；通知中心有未讀樣式、單筆點擊標記已讀、「全部標為已讀」；頁首鈴鐺顯示未讀數 Badge，點通知會前往對應頁面。

⚠️ 部署需求：`.env` 設定 `CRON_SECRET`，由外部排程每 15–60 分鐘呼叫 `POST /api/push/dispatch`（帶 `x-cron-secret` header）。

3. **旅程狀態尚未成為真正的操作模式** — ✅ 已完成

- 「開始旅程／結束旅程」流程（管理者），含確認對話框與 Toast 回饋。
- 旅途中模式首頁：行動優先顯示「下一站、重要物品待確認、待付款」三張卡片。
- 已完成旅程顯示唯讀紀錄橫幅，管理者可重新開啟。
- 已完成狀態擴及各子頁面：行程、行李、待辦、重要事項的新增表單改為唯讀提示，編輯／刪除／勾選收起；費用隱藏新增與編輯／刪除；成員隱藏邀請按鈕。結算付款維持可操作（旅程結束後仍需還款）。

- 旅途中模式頁面整潔：行程、行李、待辦、重要事項卡片的編輯／刪除圖示收進「⋯」二級選單（規劃中維持平鋪）。
- 旅途中首頁提供「回到規劃中」與「結束旅程」按鈕，三種狀態可雙向切換。

4. **使用者設定頁面尚未實作** — ✅ 已完成

- `/settings` 頁面可修改暱稱與頭像網址（含預覽），透過 `PUT /api/me` 更新。
- 入口在旅程列表頁的「設定」按鈕。

### P1：主要功能只完成一部分

5. **AI Agent 的進階能力尚未完整** — 🟡 大部分完成

✅ 已完成：

- **變更提案流程**：`使用者要求 → AI 建立提案 → 介面顯示提案卡片 → 使用者確認 → 寫入 → 可復原`。AI 不再直接寫入資料；提案存在 `agent_action` 資料表，狀態為待確認／已執行／已取消／已復原，重新開啟對話仍可看到提案狀態。
- 工具擴充：新增行程（可用 `placeName` 連結旅程已儲存的地點）、**修改行程**、**刪除行程**、新增待辦、新增重要物品。修改與刪除的復原會還原先前欄位或整列資料。
- **地點查詢工具** `search_place`：即時查詢真實地點名稱、地址與座標（OpenStreetMap／Nominatim，唯讀免確認）。
- AI 上下文加入行程 id（供修改／刪除引用）與地點營業時間、評價資料。

❌ 仍缺：

- 即時營業時間／評價的外部來源（目前僅使用旅程內已儲存的資料；Nominatim 不提供）。
- 結構化批次匯入完整行程（一次多筆提案可行，但沒有專用匯入格式）。
- 交通方式與行程順序的調整。

6. **手機版 PWA 只完成基礎** — 🟡 部分完成

✅ 已完成：

- 手機固定底部導覽（總覽／行程／費用／行李／更多），「更多」選單收納待辦、重要事項、成員、AI、通知，所有頁面皆可到達；含 safe-area 內距，桌面保留頂部導覽。
- 頂部與底部導覽的當前頁面選中狀態（`aria-current` + 視覺高亮）。
- 離線狀態提示：斷線時全站頂部顯示「目前離線」橫幅，恢復連線自動消失。
- Leaflet CSS 與地圖標記圖示改由本地打包（原為 unpkg CDN），離線時地圖介面不再破圖。

❌ 仍缺：

- 離線新增／修改後重新連線同步。
- 資料過期（快取內容）提示。
- 單手操作最佳化。

## 已完成，但建議改善

### 分帳系統

這是目前完成度最高的區域，已有明確的合計驗證、逐項分帳、圖表和結算。分攤方式維持平均、百分比、固定金額三種（已決議不做自訂份數，記得同步修正 [project.md](/C:/Users/whitecloud/Documents/code/PackSync/project.md:8) 的描述）。

建議改善（未動工）：

- 將複雜表單拆成「金額 → 付款人 → 分攤 → 確認」步驟。
- 即時顯示「最後每個人應付多少」。
- 儲存後提供成功提示與復原。
- API 失敗時不要停留在沒有反應的狀態。

### 行李、待辦、重要物品 — 🟡 大部分完成

✅ 已完成：

- 行李完成進度（`8 / 12 已完成`）、一鍵全部勾選／取消。
- 待辦依「已逾期／今天／接下來／沒有期限／已完成」視覺分組，逾期日期紅字。
- 重要事項顯示「誰確認、何時確認」，並可直接標記「我已確認」／取消確認。
- 行李與待辦刪除後提供短時間復原。

❌ 仍缺：

- 批次新增物品。
- 將重要物品確認直接整合進「下一個行程」卡片（旅途中首頁已有入口，行程卡片內尚未）。

### 成員與邀請 — ✅ 已完成

- `owner/admin/member` 改為中文標籤（擁有者／管理員／成員），含頁首角色徽章。
- 只有擁有者／管理員看得到「建立邀請連結」按鈕與有效邀請清單。
- 有效邀請顯示到期時間，可複製、撤銷、重新產生（撤銷舊連結後建立新的）。
- 成員卡片「⋯」選單：擁有者可設為管理員／成員、轉移擁有權（自己降為管理員）、移除成員；管理員可移除一般成員。
- 非擁有者可「退出旅程」，退出後導回旅程列表。

### 全站操作回饋 — ✅ 大致完成

- 全域 Toast 系統（成功／失敗／可帶復原按鈕）與產品內確認對話框，掛在根 layout。
- 已接上：行程、行李、待辦、重要事項、費用（重算結算、結算狀態、刪除費用）、成員（邀請）、AI 對話刪除。原生 `confirm()` 已全數移除。
- 導覽列當前頁 `aria-current` 和視覺狀態。

❌ 仍缺：

- 表單欄位層級的錯誤提示、網路中斷提示與重試。
- 儲存中、空狀態、載入失敗三種明確狀態的全站統一。

## 剩餘工作建議順序

1. 離線新增／修改後重新連線同步、快取資料過期提示。
2. 月曆型日曆檢視、路線依交通方式區分（步行／大眾運輸）。
3. AI 批次匯入行程格式、行程順序調整、外部營業時間／評價來源。
4. 表單欄位錯誤提示與載入／空狀態統一。
5. 分帳表單分步驟、批次新增行李物品等小型改善。

另外已完成：成員、費用（付款人／分攤明細／轉帳／新增表單）等處的使用者顯示已改用頭像（有設定時顯示圖片，否則顯示首字母，共用 `user-avatar.svelte` 元件）。

總結來說：**分帳、行程（日期切換／時間表檢視／地圖連動／真實路線）、行李、待辦、重要物品、成員與邀請管理（含退出、擁有權轉移、頭像顯示）、AI 提案確認與復原流程（含修改／刪除行程與地點查詢）、旅程狀態三態切換（含旅途中收整與子頁面唯讀）、使用者設定、通知系統（偏好、排程推播、事件推播、已讀未讀與 Badge）、手機底部導覽、離線提示與 Leaflet 本地化、全站 Toast／確認回饋已完成；離線同步、月曆檢視、AI 批次匯入、表單欄位錯誤提示仍未完成。**

